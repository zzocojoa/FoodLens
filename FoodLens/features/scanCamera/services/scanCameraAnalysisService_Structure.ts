/**
 * Structure-only facade for `scanCameraAnalysisService.ts`.
 * Re-exports data shapes (types/interfaces) to keep structure concerns isolated.
 */
export type * from './scanCameraAnalysisService';
