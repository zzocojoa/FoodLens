/**
 * Structure-only facade for `locationPermissionService.ts`.
 * Re-exports data shapes (types/interfaces) to keep structure concerns isolated.
 */
export type * from './locationPermissionService';
