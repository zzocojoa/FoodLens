# Food Canary Images

This directory is for local paid food canary images only. The image files are
ignored by git and should not be included in PRs.

Use the filenames referenced by `../scaffold_manifest.json`, or update the
manifest before running the paid food golden eval harness.

Do not commit private or user-identifying photos.
